# Code review — 2026-08-09-gateway-cancel-keeps-paid-period — round 2
Reviewer: Opus (strict-reviewer)
Date: 2026-08-10
Scope:
- `apps/jarvis_admin_v2` (branch `fix/autopay-cancel-keeps-paid-period`, uncommitted):
  `billing/cancellation.py`, `billing/webhook.py`, `billing/cashfree_webhook.py`,
  `api/account.py`, `billing/gateway_cancel_defect_report.py` (new),
  `tests/billing/test_autopay_cancel.py` (new),
  `tests/billing/test_gateway_cancel_defect_report.py` (new),
  `tests/billing/test_terminal_cancel_autorenew.py`, `tests/billing/test_trial_autopay.py`,
  `tests/billing/test_cashfree_subscription.py`, `tests/api/test_account_endpoints.py`,
  `tests/api/test_authority_repair_hardening.py`. (`www/dashboard.html` excluded as instructed.)
- `apps/jarvis`: `frontend/src/components/settings/PlanBillingPane.vue`,
  `frontend/src/components/settings/PlanBillingPane.spec.js`.
- Read for context (unchanged, load-bearing): `billing/_activation.py`, `billing/expiry.py`,
  `billing/signup.py`, `billing/_preflight.py`, `billing/capabilities.py`,
  `billing/providers/{razorpay,cashfree}.py`, `billing/{razorpay,cashfree}_client.py`,
  `api/_responses.py`, `fleet/_tenant_lookup.py`, doctype `Jarvis Subscription` (status options).

Tests executed on `test_site` only (never `jarvis_admin_v2.local`, no pool/provision/fleet module):
`test_autopay_cancel` 31 OK · `test_terminal_cancel_autorenew` 9 OK · `test_trial_autopay` 44 OK ·
`test_gateway_cancel_defect_report` 14 OK · `test_cashfree_subscription` 39 OK · `test_webhook` 16 OK ·
`test_cashfree` 11 OK · `test_expiry` 10 OK · `test_notifications` 8 OK · `test_activation` 19 OK ·
`test_reactivation_apply` 20 OK · `test_expiry_banners` 20 OK · `test_billing_contract` 3 OK ·
`test_billing_payload` 37 OK · `test_contract_fixture_drift` 2 OK · `test_onboarding_contract` 3 OK ·
`test_incident_ops` 4 OK · `test_dispute_webhook` 42 OK · `api.test_account_endpoints` 61 OK ·
`api.test_authority_repair_hardening` 6 OK · `api.test_console` 46 OK.
All re-run green AFTER the flow review's teardown. `ruff check` / `ruff format --check` clean (465 files).
Bench `npx vitest run` 1021 OK / 64 files.

**Environment note, not a finding against this diff.** `jarvis_admin_v2.tests.billing.test_signup`
fails on this machine with 14–21 random `QueryDeadlockError`s per run (victims differ every run;
deadlocking statements are fixture `DELETE FROM tabJarvis Customer` / `INSERT INTO tabDefaultValue`).
It is not this change: `test_signup.py` and `billing/signup.py` reference none of the changed symbols
(`mandate_died`, `_subscription_cancelled`, `cancel_plan_at_period_end`, `_may_cancel`,
`_subscription_status_changed`), and any single test from the module passes standalone. Flagging it
because the round-2 hand-off claimed "504 tests green across 14 modules" and that number is not
reproducible here.

## Findings

| # | Severity | Location | What breaks | Required fix |
|---|----------|----------|-------------|--------------|
| 1 | **MAJOR** | `billing/cashfree_webhook.py:385` (`sub = _sub_for_mandate(event)`) resolving through `:255-266`; contrast `:375` and `:420` | **The MAJOR-2 fix is unreachable on the payload shape this module itself documents as real, so provider parity is asserted but not delivered.** `_subscription_status_changed` reads the status tolerantly — `details.get("subscription_status") or data.get("subscription_status")` (`:376`) — and its sibling `_activate_authorized_mandate` resolves the mandate id tolerantly — `details.get("subscription_id") or data.get("subscription_id")` (`:420`) — with a docstring that states the nested form is the *verified* real shape for this exact event (`:406-409`). But `_sub_for_mandate` (`:259`) reads **only** the flat `data["subscription_id"]`, so on the nested payload it returns `None` and the whole dead-mandate branch — the pre-existing `autorenew=0` **and** the new `_mark_past_due` parity added by this change — never executes. Proven live over real HMAC-signed HTTP on `test_site` against one fixture, one event type, one status: **flat id → `Past Due` + `autorenew=0`** (flow S26); **nested id → nothing at all**, `{"ok":true}` and the row still `Active/autorenew 1` (flow S27); and the ACTIVE branch of the *same* nested event resolved it fine and stamped `cashfree_auth_payment_id` (flow S28). Nothing in the suite can catch this: `TestCashfreeOnHold`, `TestCashfreeDeadMandateDunning` and `TestProviderParity` all `patch.object(cashfree_webhook, "_sub_for_mandate", ...)`, and the only HTTP-level test of the branch (`test_cashfree_subscription.py:623-629`) sends a hybrid — flat id, nested status — that no comment in the repo claims is real. Plan edge 15 was amended *in round 1* specifically to promise this parity; it is not verifiable at the boundary. | Make `_sub_for_mandate` read `((event.get("data") or {}).get("subscription_details") or {}).get("subscription_id") or (event.get("data") or {}).get("subscription_id")`, i.e. the same tolerance its sibling three functions away already has. Then add one Cashfree dead-mandate test that goes through the `cashfree_webhook()` entry point with the **nested** shape (`_status_active_body()`'s shape with `subscription_status: "CANCELLED"`) and asserts `autorenew=0` + `Past Due`, unpatched. Until a test drives the real resolver, the parity claim is unpinned. |
| 2 | MINOR | `billing/webhook.py:537-553`; `billing/cancellation.py:150-151` | Plan edge 5 says *"The existing log line is kept."* It was not. The deleted branch logged `subscription.cancelled echo for pending-cancel {sub.name}; entitlement preserved until {…}`; now an echo landing on a sub whose `autorenew` is already 0 — the customer's own period-end cancel, a redelivery, or the echo of a bench cancel — returns from `mandate_died` **before** the logger call and leaves no trace at all. Three of the plan's edge cases (5, 8, 21) are now silent no-ops with no audit line. `test_echo_of_customers_own_period_end_cancel_is_untouched` asserts row state, never the log. | Either log the no-op case in `mandate_died` (a single `debug`/`info` naming sub + reason + "already off") or amend plan edge 5 through `planner` to drop the requirement. Silence chosen by accident is not the same as silence chosen. |
| 3 | MINOR | `tests/billing/test_autopay_cancel.py:262-283` | `test_pending_payment_sub_cancel_only_clears_autorenew` is titled "Edge 11" — the edge case that made round 1 RED — and **cannot fail on it**. Its assertion is `assertNotIn(sub.status, ("Active", "Cancelled"))`, which passes with the BLOCKER fully present (`Past Due` is in neither). Worse, its docstring still describes the pre-fix behaviour as correct: *"it falls through to Past Due (never Active, never Cancelled/Suspended), same as `_subscription_halted` already does for this state"* — the exact reasoning the fix overturned, left in the file next to the fix. The genuine cover is `TestGatewayCancelThroughRealResolver::test_pending_payment_signup_stage_stays_resumable`. | Assert `sub.status == "Pending Payment"` and rewrite the docstring to the post-fix rule, or delete the test as superseded. A test that documents the bug as intended behaviour is worse than no test. |
| 4 | MINOR | `billing/cancellation.py:116-121` | `still_entitled` is dead code after this change — `webhook.py` was its only caller and the diff dropped that import; `grep` finds no production caller and no test anywhere in the app. The diff did not delete it; it *broadened* its docstring (from "True while a **pending-cancel** sub is inside the period it already paid for. Used by the webhook echo guard" to "True while a sub is inside the period it already paid for"), which reads as if it were still the shared predicate the module header promises every reader imports. | Delete it, or state that it is retained for external/future callers. |
| 5 | MINOR | `billing/webhook.py:133` (`_sub_for_rzp_subscription`), reached from the rewritten `_subscription_cancelled:537` | A validly-signed but structurally-malformed `subscription.cancelled` body 500s the webhook. Verified live: `{"event":"subscription.cancelled","payload":{"subscription":[]}}` → `AttributeError: 'list' object has no attribute 'get'` → dispatch savepoint rollback → HTTP 500 `WebhookDispatchFailed`, which is precisely the ~24h Razorpay retry storm plan edge 9 exists to prevent. Seven other hostile bodies (empty entity, null id, SQL fragment, 5 000-char id, dict-as-id, RTL/zero-width unicode, missing `payload`) all correctly returned 200-and-skip. Pre-existing and outside the diff's changed lines — but plan edge 10 claims *"Malformed payload … No crash"* and this change's edge-10 test covers only a NULL `current_period_end`. | Coerce defensively in `_sub_for_rzp_subscription` (`sub = payload.get("subscription"); entity = sub.get("entity") if isinstance(sub, dict) else {}`) or record in the plan that "malformed payload" means field values, not payload structure. |

## Edge-case verification

| Plan edge case | Handling site | Test | Verified |
|---|---|---|---|
| 1. Mid-period gateway cancel stays Active, entitled | `webhook.py:537-553`; `_mark_past_due` guard `:702-711` | `test_terminal_cancel_autorenew.py::test_gateway_cancelled_event_clears_autorenew` (now asserts status + `Customer.status` + `User.enabled` + period end), `test_autopay_cancel.py::test_active_with_time_left_stays_active` (real resolver), `test_trial_autopay.py::test_cancelled_disables_autopay_without_revoking` | YES — and live over signed HTTP (flow S2: `Active/0/has_mandate F/can_reauthorize T/can_cancel F`, customer `Active`, `User.enabled=1`, period unmoved) |
| 2. Gateway cancel after the period ran out → Past Due, grace, then Expired | `webhook.py:552-553` + `expiry.py` | `test_cancel_after_period_end_marks_past_due`, `test_active_past_period_end_becomes_past_due` (real resolver) | YES — live S3 (`Past Due`, `can_reactivate: true` immediately) and S21b/d/e (grace 7d honoured, then Expired) |
| 3. `subscription.completed` non-destructive | `webhook.py:112-118` dispatch | `test_subscription_completed_is_non_destructive`, `test_trial_autopay.py::test_completed_is_non_destructive` | YES — live S6 |
| 4. Echo of an operator console cancel | `webhook.py:538` status guard | `test_second_terminal_cancel_is_a_no_op` (now asserts `mandate_died` not called — a real assertion, unlike the deleted `_revoke_stop_target` mock) | YES — live S5: row stayed `Cancelled/autorenew 1`, customer `Suspended` |
| 5. Echo of the customer's own period-end cancel | `cancellation.py:150` | `test_echo_of_customers_own_period_end_cancel_is_untouched` | State YES; **log line NOT kept — finding 2** |
| 6. Echo of a SUPERSEDED mandate | Defence is the id-swap ordering (`_billing_actions.py`, `account.py`); the unreachable guard was correctly **deleted** and `webhook.py:530-535` now names the ordering | `test_reauthorization_writes_the_new_id_before_enqueuing_the_old_ones_release` (pins the ordering via a `frappe.enqueue` side_effect that reads the row's id at enqueue time — this one *can* fail); `test_a_superseded_id_resolves_to_no_row_through_the_real_resolver` (unpatched resolver) | YES — round-1 finding 3 properly closed; live S23: after the row adopted `sub_..._NEW`, the old id's echo left `autorenew=1` and the new id intact |
| 7. Full refund / lost dispute stay terminal | `webhook.py:556+` → `incidents.revoke_entitlement_for_money_loss` | `test_trial_autopay.py` refund/dispute regressions; `test_dispute_webhook` 42 OK | YES — live S13: sub `Cancelled`, customer `Suspended`, `User.enabled=0` |
| 8. Redelivered / duplicate event | `cancellation.py:150` | `test_redelivered_event_is_idempotent` (asserts `db_set` not called) | YES — live S2b (same event id → `{"idempotent": true}`; fresh id → no further write) |
| 9. Unknown subscription id → skip, never raise | `webhook.py:129-141` | `test_unknown_subscription_id_is_skipped_not_raised` | YES — live S14 |
| 10. Malformed payload / NULL `current_period_end` | `webhook.py:702-711` | `test_null_period_end_marks_past_due_without_crash` | PARTIAL — NULL period end YES; structurally-malformed payload 500s (finding 5) |
| 11. `Pending Payment` sub — `autorenew=0` **only** | `webhook.py:552` `if sub.status == "Active"` | `TestGatewayCancelThroughRealResolver::test_pending_payment_signup_stage_stays_resumable` (real resolver + asserts `_signup_payment_state` is not `SIGNUP_TERMINAL`) | **YES — round-1 BLOCKER closed.** Live end-to-end (S1a/b/c, S16, S16b) and with a negative control (S17) proving the scenario can still fail. The weak duplicate at `:262` is finding 3. |
| 12. Concurrency / one batched `db_set` | `cancellation.py:152` | `test_mandate_died_writes_in_a_single_batched_call` | YES (note, unchanged from r1: `_subscription_cancelled` still issues a second `db_set` via `_mark_past_due` on the lapsed branch — same shape `_subscription_halted` always had). Live S24: a bench cancel and a gateway cancel fired concurrently on one sub both returned 200 and converged on `Active/0/cancelled_at NULL` |
| 13. No outbound gateway/container call on this path | imports of `stop_tenant` / `resolve_authoritative_tenant` deleted from `webhook.py` | `test_mid_period_gateway_cancel_makes_no_outbound_calls` — round-1 finding 9 closed: the vacuous `cancellation.release_live_mandate` patch is gone, replaced by `patch("frappe.enqueue")`, which this path really can reach | YES |
| 14. Auth boundary unchanged | `webhook.py:25-33` | `test_bad_signature_rejects_a_cancel_shaped_body` | YES — live S7: HTTP 400 `BadSignature`, nothing written |
| 15. Cashfree `ON_HOLD` is a pause, never dunned; dead siblings DO dun on the same Active-only gate | `cashfree_webhook.py:383-396` | `test_on_hold_clears_autorenew_without_past_due`, `TestCashfreeDeadMandateDunning` (2 tests), `TestProviderParity::test_razorpay_and_cashfree_lapsed_cancel_match` | **NO — finding 1.** The *logic* is right and I verified all of it live on the flat payload (S26, S29–S34, incl. ON_HOLD never dunned mid-period or lapsed, Expired not resurrected, Pending Payment not dunned, redelivery idempotent). But on the nested payload the module documents as real, the branch does not run at all (S27), and every test patches the resolver away or feeds the flat id. |
| 16. Already-damaged rows not auto-repaired | `webhook.py:538` | `test_already_cancelled_row_is_not_auto_repaired`; report script + 14 tests | YES — live S5 |
| 17. Bench "Cancel auto-renewal" (mandate customer) | `account.py:558-594` | `test_cancel_autorenewal_leaves_mandate_customer_active` | YES — live S21a over real HTTP with customer auth: `Active`, `autorenew 0`, `cancelled_at NULL`, `cancel_at_period_end 0`, `has_mandate false`, then `can_reauthorize true` |
| 18. Bench "Cancel subscription" (one-shot) unchanged | `account.py:569` | `test_cancel_subscription_one_shot_is_unchanged`; `test_account_endpoints` 61 OK | YES — live S10/S10b: `cancelled_at` stamped, `cancel_at_period_end 1`, Resume still works and restores `can_cancel: true` |
| 19. Double-click / re-cancel on the AutoPay branch | `account.py:558` (`_may_cancel` before any gateway call) | `test_double_click_cancel_autorenewal_does_not_release_twice` | YES — live S8 with two genuinely parallel HTTP requests: both OK, identical payloads, `cancelled_at` still NULL |
| 20. Release fails at the gateway | `account.py:584-591` + provider swallow | `test_release_failure_at_gateway_leaves_local_cancel_intact` | YES — round-1 finding 4 closed: `frappe.db.commit()` now sits between the local write and `release_live_mandate`, matching `signup.py`'s durable-before-unbounded-call pattern. Both providers' releases swallow (`razorpay_client.release_mandate:397-403`, `cashfree_client.release_mandate_id:381-387`), so the endpoint still returns OK |
| 21. Cancel AutoPay, then the gateway echo arrives | `cancellation.py:150` | `test_gateway_echo_after_bench_cancel_is_a_noop` | YES — live S24 (both directions, concurrently) |
| 22. Cancel AutoPay then immediately re-arm | `_may_reauthorize` `account.py:466-477`; ordering per edge 6 | `test_a_superseded_id_resolves_to_no_row_through_the_real_resolver` | YES — round-1 finding 10 closed. Live S12 (`reauthorize_autopay` demands no Resume) and S23 (the released mandate's echo cannot disarm the replacement) |
| 23. AutoPay cancelled while `Past Due` | `account.py:489` | `test_autopay_cancel_while_past_due_keeps_grace` | YES — live S11: OK, no `cancelled_at`, still `Past Due` |
| 24. AutoPay cancelled during a scheduled downgrade | `account.py:566-569` (never touches `scheduled_plan`) | `test_autopay_cancel_during_scheduled_downgrade_leaves_it_untouched` | YES |

### Round-1 findings — disposition

| r1 | Status |
|---|---|
| 1 BLOCKER (signup-stage dunned) | **CLOSED.** The `if sub.status == "Active"` gate is the right shape, and narrower than my suggestion — I withdraw `("Active", "Past Due")`: the developer is correct that `Past Due` is entitled and an `Expired` row must not be resurrected into it. `Jarvis Subscription.status` has exactly six values (Pending Verification / Pending Payment / Active / Past Due / Cancelled / Expired) and every non-Active one is now excluded; the only writer of `status="Active"` in the app is `_activation.py:159`, which always sets a non-NULL `current_period_end`, so the gate cannot silently skip a genuinely-lapsed sub. Verified live on all four shapes (S1, S3, S4, S25). |
| 2 MAJOR (Cashfree parity) | **NOT CLOSED — finding 1.** The dunning added is correct and I verified it live, but the branch it was added to cannot resolve its subscription on the documented real payload, and no test can detect that. |
| 3 MINOR (dead superseded-id guard) | CLOSED. Guard deleted, `webhook.py:530-535` names the ordering, plan edge 6 amended, and both replacement tests can genuinely fail. |
| 4 MINOR (commit before gateway release) | CLOSED. |
| 5 MINOR (report over-asserts EXCLUDED_OPERATOR) | CLOSED. `_classify:324-339` reports UNCERTAIN when gateway evidence exists, the trade-off is written down at `:275-305`, and `test_operator_modified_row_with_a_release_echo_is_uncertain_not_excluded` pins it. |
| 6 MINOR (unbounded log read + N+1) | CLOSED. `_load_relevant_webhook_events` is `creation >=` bounded, `_bulk_find_assigned_tenants` is one query whose filters I checked line-for-line against `find_assigned_tenant` (`assignment_state=Assigned`, `status in`, `is_move_target=0`, `quarantined=0`, `assigned_at desc`) — they match. `window_days=365`: **challenged and accepted.** The window must outlive the defect, not a billing cycle; the event set is 2 rare types plus refunds/disputes, so the read stays cheap; and a candidate outside the window reports UNCERTAIN rather than being dropped, which is the safe direction for a report that decides reinstatement. `test_events_older_than_the_window_are_not_used_as_evidence` and `test_window_days_must_be_positive` cover it. |
| 7 MINOR (`mandate_died` needs a Document) | CLOSED — stated in the docstring at `cancellation.py:127-128`. |
| 8 MINOR (`can_cancel` hard gate, no legacy fallback) | CLOSED. `PlanBillingPane.vue:212-214` uses `?? (!cancelling && !ended)`, and the spec pins all three: `can_cancel:false` beating the legacy rule, absent-field fallback, and absent-field + cancelling/ended. |
| 9 MINOR (vacuous patch) | CLOSED. |
| 10 MINOR (nothing drove the real resolver) | CLOSED — `TestGatewayCancelThroughRealResolver` (4 tests) plus two more unpatched cases elsewhere in the file. |

VERDICT: RED
