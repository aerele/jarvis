{
 "actions": [],
 "allow_rename": 0,
 "autoname": "field:agent_slug",
 "creation": "2026-07-04 20:30:00.000000",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "agent_slug",
  "title",
  "description",
  "category",
  "nature",
  "delivery",
  "version",
  "publisher",
  "status",
  "tools_required",
  "doctypes_required",
  "rule_tokens",
  "min_apps",
  "rule_pack",
  "writes",
  "skill_bundle",
  "default_schedule",
  "validated_for_fy",
  "allowed_roles",
  "allowed_users"
 ],
 "fields": [
  {
   "fieldname": "agent_slug",
   "fieldtype": "Data",
   "label": "Agent Slug",
   "reqd": 1,
   "unique": 1,
   "in_list_view": 1,
   "in_standard_filter": 1,
   "description": "Stable registry key (e.g. close-auditor). Used as the doc name (naming_rule By fieldname) so a re-sync is idempotent."
  },
  {
   "fieldname": "title",
   "fieldtype": "Data",
   "label": "Title",
   "in_list_view": 1
  },
  {
   "fieldname": "description",
   "fieldtype": "Long Text",
   "label": "Description"
  },
  {
   "fieldname": "category",
   "fieldtype": "Data",
   "label": "Category",
   "in_standard_filter": 1,
   "description": "Domain slug (audit, compliance, close, ap, ar, bank-recon, analytical-review)."
  },
  {
   "fieldname": "nature",
   "fieldtype": "Select",
   "label": "Nature",
   "options": "Auditor\nOperator\nScribe",
   "in_standard_filter": 1
  },
  {
   "default": "legacy",
   "fieldname": "delivery",
   "fieldtype": "Select",
   "label": "Delivery",
   "options": "legacy\ndelegate",
   "in_standard_filter": 1,
   "description": "How the agent's SKILL body reaches the container. 'legacy': the body is stored in skill_bundle on this row and pushed from the bench (old path). 'delegate': the body NEVER enters the customer DB — the bench emits only an enablement signal and admin looks the body up from the bundle store (A2). Set by the catalog sync from registry.delivery; empty/absent registry = legacy."
  },
  {
   "fieldname": "version",
   "fieldtype": "Data",
   "label": "Version"
  },
  {
   "fieldname": "publisher",
   "fieldtype": "Data",
   "label": "Publisher"
  },
  {
   "default": "Draft",
   "fieldname": "status",
   "fieldtype": "Select",
   "label": "Status",
   "options": "Draft\nPublished\nDeprecated\nComing Soon",
   "in_list_view": 1,
   "in_standard_filter": 1
  },
  {
   "fieldname": "tools_required",
   "fieldtype": "Small Text",
   "label": "Tools Required (JSON)",
   "description": "JSON array of jarvis__* tool ids the agent needs."
  },
  {
   "fieldname": "doctypes_required",
   "fieldtype": "Small Text",
   "label": "DocTypes Required (JSON)",
   "description": "JSON array of DocTypes the run-as user MUST hold read on for this agent's aggregates to be numerically correct (A12). Checked at install/validate against the mapped run_as_user (fail-closed on a missing read). Declared here so it is checkable without leaking rule shape."
  },
  {
   "fieldname": "rule_tokens",
   "fieldtype": "Small Text",
   "label": "Rule Tokens (JSON)",
   "description": "A2/A16 id-only rule token set for a delegate auditor: the OPAQUE tokens the container evaluator emits and record_agent_run validates a finding's rule against. Tokens only — NO rule bodies, statements, thresholds or predicates ever reach the bench. Empty for operators / legacy agents. Synced from registry.rule_tokens (itself mirrored from the bundle store's rules.ids.json)."
  },
  {
   "fieldname": "min_apps",
   "fieldtype": "Small Text",
   "label": "Min Apps (JSON)",
   "description": "JSON array of required Frappe apps, e.g. [\"erpnext\"]."
  },
  {
   "fieldname": "rule_pack",
   "fieldtype": "Data",
   "label": "Rule Pack",
   "description": "R5-J11(c) canonical pack membership id for this agent (curated pack map: the 14 domains map onto the 7 packs + measurement). Declarative, non-IP — it is a pack NAME only, never the as-coded predicate. Synced from registry.rule_pack. The reviewer two-pack activation-ceiling gate counts DISTINCT non-empty rule_pack values (never the agent slug), so an empty pack id contributes nothing to the count."
  },
  {
   "fieldname": "writes",
   "fieldtype": "Small Text",
   "label": "Writes (JSON)",
   "description": "R5-J9 declarative operator write contract: JSON array of {doctype, mode} (mode: draft|approval-request; an optional operation/operations key narrows to create|update) the operator is permitted to write. Non-IP metadata mirrored from the bundle-store manifest.writes[]. NULL/empty for auditors (which are refused any write). create_doc/update_doc bind a delegate call to this contract BEFORE Frappe permission checks: a delegate may only create/update a declared doctype, only docstatus==0, and updates only on rows it owns."
  },
  {
   "fieldname": "skill_bundle",
   "fieldtype": "Long Text",
   "label": "Skill Bundle (JSON)",
   "permlevel": 1,
   "description": "JSON list of {path, body} SKILL.md payloads pushed to the container on Apply. permlevel 1 (SM-only; PART 3 TASK 33): the proprietary agent rule-pack is vendor IP, so a normal Jarvis User cannot read it via generic REST / get_agent. The catalog seed writes it via ignore_permissions and the engine reads it via get_doc (both bypass permlevel)."
  },
  {
   "fieldname": "default_schedule",
   "fieldtype": "Small Text",
   "label": "Default Schedule (JSON)",
   "description": "JSON {schedule_enabled, schedule_frequency} applied when the agent is installed."
  },
  {
   "fieldname": "validated_for_fy",
   "fieldtype": "Data",
   "label": "Validated For FY",
   "description": "The fiscal year the (statutory) rule set was last legal-reviewed for. Used to gate stale statutory packs."
  },
  {
   "fieldname": "allowed_roles",
   "fieldtype": "Table MultiSelect",
   "label": "Allowed Roles",
   "options": "Jarvis Agent Allowed Role",
   "description": "Roles allowed to install/run this agent. Access is DENY BY DEFAULT: a listing with no allowed roles AND no allowed users is reachable by Jarvis Admins only. Bench-admin state, NEVER written by the catalog sync."
  },
  {
   "fieldname": "allowed_users",
   "fieldtype": "Table MultiSelect",
   "label": "Allowed Users",
   "options": "Jarvis Agent Allowed User",
   "description": "Named users allowed to install/run this agent, on top of any allowed roles. Access is DENY BY DEFAULT: a listing with no allowed roles AND no allowed users is reachable by Jarvis Admins only. Bench-admin state, NEVER written by the catalog sync."
  }
 ],
 "index_web_pages_for_search": 0,
 "links": [],
 "modified": "2026-09-02 12:00:00.000000",
 "modified_by": "Administrator",
 "module": "Jarvis",
 "name": "Jarvis Agent Listing",
 "naming_rule": "By fieldname",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  },
  {
   "read": 1,
   "role": "Jarvis User"
  },
  {
   "read": 1,
   "role": "Jarvis Admin",
   "write": 1
  },
  {
   "permlevel": 1,
   "read": 1,
   "role": "System Manager",
   "write": 1
  }
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
